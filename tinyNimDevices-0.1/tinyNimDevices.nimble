version       = "0.1.0"
author        = "Birkemosen"
description   = "tinyNim device files"
license       = "MIT"
skipDirs      = @["doc"]

skipExt = @["nim"]

requires "nim >= 0.20.0", "compiler >= 0.20.0"
